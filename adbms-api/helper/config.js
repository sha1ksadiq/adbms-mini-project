let config = function() {
    return { 
        database: {
            host: "localhost",
            user: "root",
            password: "Madnessregent12*",
            database: "stream",
            multipleStatements: true
        }
    };
}

module.exports = new config();